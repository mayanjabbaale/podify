# Web
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9        # PDF upload form parsing

# Config & validation
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0

# Database
sqlalchemy>=2.0
alembic>=1.13

# Queue
celery>=5.6,<6                 # MUST run with -P solo on Windows
redis>=5.0

# PDF
pymupdf>=1.28,<2               # cp310-abi3 stable-ABI wheel; works on cp313

# TTS (local)
kokoro-onnx                    # import name: kokoro_onnx
soundfile>=0.12

# Audio assembly
pydub>=0.25                    # ffmpeg must be on PATH for mp3 output
audioop-lts>=0.2               # stdlib `audioop` was removed in Python 3.13;
                               # pydub's utils.py imports `pyaudioop as audioop`,
                               # and `audioop-lts` is the PyPI drop-in shim that
                               # provides the `pyaudioop` import name.

# Gemini (free tier)
google-genai>=2.20,<3          # pin <3 to avoid upcoming AFC-breaking release
tenacity>=9.0                  # retry/backoff for 429s

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
